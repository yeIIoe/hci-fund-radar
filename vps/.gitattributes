# Estes arquivos rodam no Linux: quebra de linha sempre LF, mesmo commitados do Windows.
# Sem isto o Git do Windows pode gravar CRLF e o bash da VPS quebra com "\r: command not found".
* text eol=lf
